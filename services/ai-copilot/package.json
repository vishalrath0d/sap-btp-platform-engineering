{
  "name": "ai-copilot",
  "version": "0.1.0",
  "description": "RAG copilot over ProcureIQ supplier/contract/policy documents, traced via a local Langfuse-shaped tracer. Local mode uses Ollama; SAP AI Core/Generative AI Hub is a documented, not-yet-built upgrade path (see README).",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4",
    "prom-client": "^15"
  },
  "devDependencies": {
    "jest": "^29",
    "supertest": "^7"
  }
}
